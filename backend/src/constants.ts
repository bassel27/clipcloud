import path from "path";

export const SRC_PATH = __dirname;
export const THUMBNAILS_PATH = path.resolve(SRC_PATH, 'uploads', 'thumbnails');
export const VIDEOS_PATH = path.resolve(SRC_PATH, 'uploads', 'videos');
export const IMAGES_PATH = path.resolve(SRC_PATH, 'uploads', 'images');