import path from "path";

export const SRC_PATH = path.join(__dirname, '..');
export const THUMBNAILS_PATH = path.join(SRC_PATH,'uploads', 'thumbnails');
export const VIDEOS_PATH = path.join(SRC_PATH, 'uploads', 'videos');
export const IMAGES_PATH = path.join(SRC_PATH, 'uploads', 'images');